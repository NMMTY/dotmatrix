---
title: "TableHead"
summary: "The <thead> section of a Table."
navIcon: "menu"
github: "components/Table/Table.tsx"
---

The `<thead>` section of a [Table](/components/data-display/table) — a thin styled wrapper, since
`<thead>` needs no behavior of its own beyond grouping the header row.

<Playground name="TableHead" />

## Props

<PropsTable name="TableHead" />
