---
title: "Kbd"
summary: "A single keyboard key. Always renders as a real <kbd>."
navIcon: "menu"
github: "components/Kbd/Kbd.tsx"
---

A single keyboard key, e.g. `<Kbd>⌘</Kbd><Kbd>K</Kbd>` for a shortcut hint. Always `<kbd>`.

<Playground name="Kbd" />

## Examples

Each key is its own `Kbd` — a multi-key shortcut is a small `Row` of them, not one `Kbd` with a
`+` baked into its children:

<CodeBlock
  codes={[
    {
      language: "tsx",
      code: '<Row gap="4" alignItems="center">\n  <Kbd>⌘</Kbd>\n  <Kbd>K</Kbd>\n</Row>',
    },
  ]}
  preview={
    <Row gap="4" alignItems="center">
      <Kbd>⌘</Kbd>
      <Kbd>K</Kbd>
    </Row>
  }
/>

## Props

<PropsTable name="Kbd" />
